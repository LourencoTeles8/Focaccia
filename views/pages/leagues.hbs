<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Leagues</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        html, body {
            height: 100%;
            margin: 0;
        }

        body {
            background-image: url('https://img.freepik.com/premium-vector/soccer-ball-football-balls-seamless-pattern-sports-equipment_627372-1303.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            color: white; 
        }

        body::before {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: inherit;
            background-size: inherit;
            background-position: inherit;
            background-attachment: inherit;
            background-repeat: inherit;
            filter: blur(8px); 
            z-index: -1; 
        }

        .text-box {
            background-color: rgba(0, 0, 0, 0.5); 
            padding: 20px;
            border-radius: 8px;
            color: white;
        }

        .league-card {
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            transition: transform 0.2s ease;
        }

        .league-card:hover {
            transform: translateY(-5px);
        }

        .league-info {
            display: flex;
            flex-direction: column;
        }

        .league-name {
            font-size: 1.2rem;
            font-weight: bold;
        }

        .form-control {
            background-color: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 5px;
        }

        .form-control:focus {
            background-color: rgba(255, 255, 255, 0.3);
            border-color: rgba(255, 255, 255, 0.6);
            box-shadow: 0 0 5px rgba(255, 255, 255, 0.5);
        }

        .btn-primary {
            background-color: #4CAF50;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
        }

        .btn-primary:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="text-box">
            <h1>Search Leagues</h1>
        </div>
        <hr>

        <div class="text-box">
            <form method="POST" action="/leagues/search">
                <div class="mb-3">
                    <label for="teamname" class="form-label">Team Name</label>
                    <input type="text" name="teamname" id="teamname" class="form-control" placeholder="Enter team name" required />
                </div>
                <button type="submit" class="btn btn-primary">Search</button>
            </form>
        </div>

        <hr>

        <div class="text-box">
            <h2>Results</h2>
            <div class="row">
                {{#each leagues}}
                <div class="col-md-4 mb-4">
                    <div class="league-card">
                        <div class="league-info">
                            <h5 class="league-name">{{league.name}}</h5>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>