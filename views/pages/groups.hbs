<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Groups</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('https://static.vecteezy.com/system/resources/previews/010/921/146/original/bright-cute-pattern-with-soccer-balls-on-blue-background-seamless-pattern-with-soccer-for-decoration-design-football-wallpaper-illustration-vector.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            color: white; 
            position: relative;
        }
        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: inherit;
            background-size: inherit;
            background-position: inherit;
            background-attachment: inherit;
            background-repeat: inherit;
            filter: blur(8px); 
            z-index: -1; 
        }
        .text-box {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 25px;
            border-radius: 10px;
            color: white;
            margin-bottom: 20px;
        }
        .no-box {
            background-color: transparent;
            padding: 0;
            border-radius: 0;
        }
        .group-card {
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            transition: transform 0.2s ease;
        }
        .group-card:hover {
            transform: translateY(-5px);
        }
        .group-actions a, .group-actions form {
            margin-right: 10px;
        }
        .btn-group {
            margin-top: 10px;
        }

        .form-control {
            background-color: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 5px;
        }

        .form-control:focus {
            background-color: rgba(255, 255, 255, 0.3);
            border-color: rgba(255, 255, 255, 0.6);
            box-shadow: 0 0 5px rgba(255, 255, 255, 0.5);
        }

        .btn-primary {
            background-color: #4CAF50;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
        }

        .btn-primary:hover {
            background-color: #45a049;
        }

        .btn-back {
            background-color: #f0ad4e;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            color: white;
        }

        .btn-back:hover {
            background-color: #ec971f;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center text-box">
            <h1>Manage Groups</h1>
        </div>
        <hr>
        <div class="text-box">
            <h2>Create New Group</h2>
            <form method="POST" action="/groups">
                <div class="mb-3">
                    <label for="name" class="form-label">Group Name</label>
                    <input type="text" name="name" id="name" class="form-control" placeholder="Enter the group name" required />
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea name="description" id="description" class="form-control" placeholder="Enter a short description" rows="4" required></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Create Group</button>
            </form>
        </div>

        <hr>
        <div class="text-box">
            <h2>Existing Groups</h2>
            <div class="row">
                {{#each groups}}
                <div class="col-md-4 mb-4">
                    <div class="group-card">
                        <h5>{{name}}</h5>
                        <p>{{description}}</p>
                        <div class="group-actions">
                            <div class="btn-group">
                                <a href="/groups/details/{{id}}" class="btn btn-info btn-sm">View Details</a>
                                <a href="/groups/edit/{{id}}" class="btn btn-warning btn-sm">Edit</a>
                                <form method="POST" action="/groups/delete/{{id}}" style="display: inline;">
                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
