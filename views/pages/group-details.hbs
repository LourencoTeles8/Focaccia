<!DOCTYPE html>
<html>
<head>
    <title>{{name}}</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        html, body {
            height: 100%;
            margin: 0;
        }

        body {
            background-image: url('https://img.elo7.com.br/product/zoom/2F9FF34/papel-de-parede-adesivo-bolas-de-futebol-rolo-de-3m-x-50cm-preta.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            color: white; 
        }

        body::before {
            content: "";
            position: fixed; 
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: inherit;
            background-size: inherit;
            background-position: inherit;
            background-attachment: inherit;
            background-repeat: inherit;
            filter: blur(8px);
            z-index: -1; 
        }

        .text-box {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 25px;
            border-radius: 10px;
            color: white;
            margin-bottom: 20px;
        }

        h1, h2, p {
            margin-bottom: 15px; 
        }

        .team-card {
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            transition: transform 0.2s ease;
        }

        .team-card:hover {
            transform: translateY(-5px);
        }

        .btn-group {
            margin-top: 10px;
        }

        .form-control {
            background-color: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 5px;
        }

        .form-control:focus {
            background-color: rgba(255, 255, 255, 0.3);
            border-color: rgba(255, 255, 255, 0.6);
            box-shadow: 0 0 5px rgba(255, 255, 255, 0.5);
        }

        .btn-primary {
            background-color: #4CAF50;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
        }

        .btn-primary:hover {
            background-color: #45a049;
        }

        .btn-secondary {
            background-color: #f0ad4e;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            color: white;
        }

        .btn-secondary:hover {
            background-color: #ec971f;
        }
    </style>
</head>
<body>
    
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center text-box">
            <h1>{{name}}</h1>
            <a href="/groups" class="btn btn-secondary">Back to Groups</a>
        </div>

        <div class="text-box">
            <p>{{description}}</p>
        </div>

        <div class="text-box">
            <h2>Teams</h2>
            <div class="row">
                {{#each teams}}
                <div class="col-md-4 mb-4">
                    <div class="team-card">
                        <h5>{{teamName}}</h5>
                        <p>Stadium: {{stadiumName}}</p>
                        <p>League: {{leagueName}}</p>
                        <p>Season: {{season}}</p>
                        <form method="POST" action="/groups/{{../id}}/teams/delete/{{teamId}}" style="display: inline;">
                            <button type="submit" class="btn btn-danger btn-sm">Remove Team</button>
                        </form>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>

        <div class="text-box">
            <h2>Add Team</h2>
            <form method="POST" action="/groups/{{id}}/teams/add">
                <div class="mb-3">
                    <label for="teamname" class="form-label">Team Name</label>
                    <input type="text" name="teamname" id="teamname" class="form-control" required />
                </div>
                <div class="mb-3">
                    <label for="leaguename" class="form-label">League Name</label>
                    <input type="text" name="leaguename" id="leaguename" class="form-control" required />
                </div>
                <div class="mb-3">
                    <label for="season" class="form-label">Season</label>
                    <input type="number" name="season" id="season" class="form-control" required />
                </div>
                <button type="submit" class="btn btn-primary">Add Team</button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
